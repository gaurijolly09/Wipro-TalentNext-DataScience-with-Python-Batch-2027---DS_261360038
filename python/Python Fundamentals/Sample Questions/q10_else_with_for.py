print("Can we use else with for? Yes, we can.")
print("Example:")

for number in range(3):
    print(f"Checking number {number}")
else:
    print("This else block executed because the for loop finished without breaking.")
